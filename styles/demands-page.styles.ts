import {Box, styled} from "@mui/material";

export const DemandsPageWrapper = styled(Box)({
    display: 'flex',
    justifyContent: 'center',
    paddingTop: '65px',
})
export const DemandsPageContainer = styled(Box)({
  display: 'flex',
  alignItems: 'flex-start',
  flexDirection: 'column',
  width: '100%',
})
